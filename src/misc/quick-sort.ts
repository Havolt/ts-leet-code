const numbers: number[] = [99, 44, 6, 2, 1, 5, 63, 87, 283, 4, 0];

// Get the pointer
// Compare all the numbers to the pointer
// Find the final place for the pointer
// Once we have this split the array to the left and right of the pointer final place
// Repeat above steps for all recursive arrays
function quickSort(array: number[], left: number, right: number) {}

function partition(
  array: number[],
  pivot: number,
  left: number,
  right: number
) {}

function swap(array: number[], firstIndex: number, secondIndex: number) {}

//Select first and last index as 2nd and 3rd parameters
quickSort(numbers, 0, numbers.length - 1);
console.log(numbers);
